//first priority : CRUD
//data layer -> able to create, delete, update, display, firebase
  //four actions, four types
//UI layer -> material UI, functional component, redux
  //GRID system -> less effort is better
  //display info, pictures (showcase box with arrow to change)
    //comments section -> scrollable
    //and save cancel button
  //display modal
  //swith picture by arrow clicking
  //CRUD button
  //submit form and cancel button
//second -> hookup firebase
//delete users -> change to posts collection
  //hook up authentication and create users collection
  //make CRUD work
//make map work
//don't implement dashboard
//make it online
export default function Item() {
  return (
   <div>
     this is item
   </div>
  )
}
